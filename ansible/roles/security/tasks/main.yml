---
# Security hardening tasks for Raspberry Pis
# Includes SSH hardening and UFW firewall configuration

- name: Include SSH hardening tasks
  ansible.builtin.include_tasks: ssh.yml
  tags: [security, ssh]

- name: Include UFW firewall tasks
  ansible.builtin.include_tasks: ufw.yml
  tags: [security, firewall, ufw]

